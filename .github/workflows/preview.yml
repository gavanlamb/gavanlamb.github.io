name: Preview

on:
  pull_request:
    branches:
      - main
  workflow_dispatch:

env:
  artifactName: Website

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    concurrency:
      cancel-in-progress: true
      group: ${{ github.head_ref }}-build
    steps:
      - uses: actions/checkout@v4
        name: Fetch repo
        with:
          ref: ${{ github.head_ref }}
          fetch-depth: 0
          fetch-tags: true
      - name: Generate version
        uses: gavanlamb/github-actions/.github/actions/generate-version@main
        id: version
      - name: Build site
        uses: gavanlamb/github-actions/.github/actions/build-hugo@main
        with:
          archiveName: ${{ env.artifactName }}
          destinationDirectory: site
          sourceDirectory: docs
  Deploy:
    name: Deploy
    runs-on: ubuntu-latest
    needs: build
    concurrency:
      cancel-in-progress: true
      group: ${{ github.head_ref }}-deploy
    environment: Preview
    steps:
      - name: Deploy ${{ env.artifactName }}
        uses: gavanlamb/github-actions/.github/actions/deploy-archive-to-s3@main
        with:
          archiveName: ${{ env.artifactName }}
          archiveDirectory: site
          awsAccessKeyId: ${{ secrets.AWS_ACCESS_KEY_ID }}
          awsSecretAccessKey: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          awsS3Bucket: ${{ vars.AWS_S3_CICD_BUCKET }}
          awsS3Path: ${{ github.repository }}/${{ github.head_ref }}/site
          awsS3Region: ${{ vars.AWS_DEFAULT_REGION }}
